# Everything the gates in tests/ need. Nothing else — the repo itself is CAD
# and PCB sources with no build step.
#
# matplotlib is deliberately NOT here: it is only needed to regenerate the
# coil-study plots (`python3 mower/coil-study/main.py`), never to run a test.
numpy==2.3.5
pytest==8.4.2
